<?xml version="1.0" encoding="utf-8"?>
<!--
  The dev backend is plain HTTP on a LAN / the emulator host alias, so cleartext has
  to be allowed for those hosts. Everything else keeps the platform default (HTTPS).

  10.0.2.2       - the Android emulator's alias for the host machine's loopback
  10.0.3.2       - the same thing under Genymotion
  localhost      - a backend reachable via `adb reverse tcp:4000 tcp:4000`
  192.168.x.x    - a backend on the same Wi-Fi as a physical device. Trim this to the
                   exact host before shipping anything publicly.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
