<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately parented on a framework theme, not a Material Components theme: the app
  is pure Compose and does not depend on com.google.android.material, so there is no
  Theme.Material3.* to inherit from. All real theming happens in ui/theme/Theme.kt.
-->
<resources>
    <style name="Theme.ThreeXDezine" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
